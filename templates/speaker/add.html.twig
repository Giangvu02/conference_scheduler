{% extends 'base.html.twig' %}

{% block body %}
<h2>Add New Speaker</h2>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-3">Save Speaker</button>
{{ form_end(form) }}
{% endblock %}